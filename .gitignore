# Xcode / build
build/
DerivedData/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcuserstate
*.xcscmblueprint

# The .xcodeproj is generated by XcodeGen from project.yml (`xcodegen generate`),
# but it is committed so the project opens in Xcode with no tooling step. project.yml
# is the source of truth: change it there, regenerate, commit both.
LensBeacon.xcodeproj/xcuserdata/
LensBeacon.xcodeproj/project.xcworkspace/

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# Secrets — there are none in this app, but never let one in by accident.
*.p12
*.mobileprovision
*.cer
AuthKey_*.p8
.env

# Machine/account-specific signing config — a real Apple Developer Team ID has no
# business in a public repo. Copy Config/Local.xcconfig.example to fill this in.
Config/Local.xcconfig

# Local raw screenshot captures for the App Store frame builder
AppStore/raw/
